import React from 'react';

const logout = () => {
    return (
        <div>
            <h3>Successful Logout</h3>
            <p>You have been successfully logged out, you may safely return to home through the navigation bar or leave.</p>
        </div>
    );
};

export default logout;