// Imports


const homePage = () => {
    //Logic needed for login

    // Fully constructed homepage
    // Will change depending on loggedIn state
    return (
        /* Main Div */
        <div>
            <div>

            </div>
        </div>
    );
};

export default homePage;